params{
reads = ""
genome_folder = ""
outdir = 'CAMDA_results'
samtools_path=""
}

process {
    executor = 'slurm'
    queue = 'test'
    clusterOptions = '--mem= --account= --ntasks-per-node= --job-name=' 
    scratch = true
    maxForks = 100
    queueSize = 100
}

